vi v[2005];
int vis[2005];
int col[2005];
bool dfs(int node, int c){
    vis[node]=1;
    col[node]=c;
    FOR(i,0,sz(v[node])){
        if(vis[v[node][i]]==0){
            if(dfs (  v[node][i],col[node]^1 )==false){
                return false;
            }
        }
        else if ( col[node]== col[v[node][i]]){
            return false;
        }
    }
    return true;
}

IN BIPARTITTE GRAPH TEST IF IT CONSISTS OF A CYCLE WITH ODD NUMBER OF NODES THEN 
THEN WHOLE GRAPH IS NOT BIPARTITE
